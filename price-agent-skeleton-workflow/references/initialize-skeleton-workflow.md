# 初始化提示词

> **本文件已合并为单一规范文件。**
>
> 完整读取并执行 [initialize-all-in-one.md](initialize-all-in-one.md)。
> 该文件包含初始化流程的全部规范（共享步骤 S1~S5、版本策略、规则查询、
> 比价项转换、枚举定义、格式规范、映射门禁、提案模板和确认写入），无需再读取其他 reference。
>
> 禁止读取 `full-skeleton-example.md` 或任何历史完整提示词作为生成范式。
