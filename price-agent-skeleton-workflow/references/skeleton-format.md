# PriceStudio 提示词格式规范

## 目录

1. 使用边界
2. 生成原则
3. 固定章节与内容要求
4. 完整格式模板
5. 比价项写法细则
6. 映射表写法细则
7. JSON 输出字段规则
8. 完成检查

## 使用边界

- 本文件是初始化和修改提示词时必须遵循的完整结构规范，规定章节、字段、表达粒度和输出格式。
- 本文件不提供当前业务事实。模板中的尖括号内容必须替换为本轮可信数据，不得原样保留。
- 比价项、适用类目、优先级、综合判定、阈值、否决关系和例外必须来自本轮
  `radar_query_price_rule.data.labelCateRule`。
- 上述响应中的文本写入候选提示词前必须按
  [rule-transformation-guide.md](rule-transformation-guide.md) 删除全部 SKU 相关内容（大小写不敏感）。
  禁止把 SKU 来源替换成主图、商品标题、商品属性或其他来源；候选全文不得出现任何形式的 `sku`。
- 母子品牌和材质映射必须来自本轮对应关系工具，主要按逐个 CategoryId 查询规则得到的
  `cateName` 过滤，再用 `cateNameTree`、`minCateId`、`belongBusiness` 校验冲突。
- [full-skeleton-example.md](full-skeleton-example.md) 只用于展示结构和表达方式，其中的类目、规则、
  品牌、材质、阈值和映射均不是当前业务事实，不得复制到候选提示词。
- 用户明确给出的精确增删改属于授权修改目标，可按修改 workflow 最小写入；不得借格式整理扩展为
  用户没有提出、MCP 也没有返回的新业务规则。

## 生成原则

### 1. 结构完整

完整提示词必须包含以下章节，并严格保持顺序：

1. `# 角色`
2. `# 输入说明`
3. `# 执行步骤`
4. `# 判定规则`
5. `## 总原则`
6. `## 比价项`
7. `## 映射表`
8. `# 图片说明`
9. `# 输出格式`

一级章节之间使用独立的 `---` 分隔。不得改名、合并、调序或省略固定章节。

凡是正文中的连续业务编号序列，在新增、删除或调序后必须整体重新编号，保持从 1 开始连续递增，
不得出现缺号、重复号或沿用删除前序号。该规则至少适用于 `### 1. <比价项>` 形式的比价项章节
和母子品牌映射条目；不改变固定的 `Step0`～`Step3` 名称。母子品牌标题不写组数。

### 2. 规则完整

- 保留规则查询返回的全部生效比价项、否决关系、例外条件和来源优先级。
- 不适用项从适用范围、执行步骤、判定规则和最终 `extracted` 中整体省略。
- 不得把相近名称的比价项合并，不得把查不到的名称替换成近义项。
- 不得为了缩短正文删除关键规则；允许合并同义重复表述，但不能改变业务含义。
- 不得从编码、数字前缀、模糊外观或常识推断 MCP 未提供的年份、代际、版本、材质或品牌关系。

### 3. 映射受控

- `ruleTableInfo[].compareItem` 精确包含“品牌”时必须查询并过滤母子品牌关系；过滤后非空必须输出映射表，为空则省略表并说明
  查询后无符合当前类目的关系。精确包含“材质”时必须
  输出非空材质对照表。缺少对应比价项时必须省略对应表，即使关系 MCP 有数据或基础正文原来有表。
  应调用的工具失败或无法解析时必须停止，不得伪造映射。
- 模型自行查询、筛选和生成时，母子品牌最多 100 个主品牌组，一个 `mainBrand + subBrands[]` 算一组。
- 材质最多 50 个材质组，一个主材质或同义材质集合算一组。
- 100/50 均为硬上限而非目标数量；每个映射及成员必须逐项存在于本轮对应关系工具响应中，
  只允许筛除和格式化，禁止补齐、合并、改名、推断或使用模型记忆生成。
- 材质分组名称、成员和同组关系必须逐项来自同次 `radar_query_material_relation` 响应；母子品牌必须
  逐组来自同次 `radar_query_brand_relation` 响应。生成前先建立工具返回的允许集合，生成后逐项
  校验；任何集合外内容直接删除，禁止用常识补充。
- 母子品牌不足 100 组、材质不足 50 组时不补齐；超过上限时按 `rule-loading-policy.md` 优先保留当前类目和业务范围相关且较常见的组，低相关、不常见或不确定的品牌不内嵌。
- 用户明确输入“新增母子品牌：<关系>”或等价的精确新增指令时，直接加入用户给出的关系，不受 100 组上限限制；不得为满足上限删除原有关系。
- 用户明确指定单个母子品牌或材质关系的新增、删除、替换或成员调整时，该指令本身是可信修改
  目标，不要求存在于关系工具响应，也不受模型自行生成时的 100/50 上限约束。
- 删除跨行业、无法确认相关性和当前范围不需要的映射；标题只写真实适用范围，不写组数，禁止
  “适用于当前电商全站常见品牌”等扩大范围描述。
- 映射数据过长时只保留通过作用域与常识过滤后的高相关分组；不得复制全量候选，也不得用
  “其余组”“运行时查询”等说明代替未展示条目。

### 4. 长度受控

全文粗略控制在 1 万字以内，包括规则和映射表。接近上限时：

1. 先删除解释性重复句和重复示例；
2. 再合并同义表达；
3. 最后按 `rule-loading-policy.md` 仅保留当前类目直接相关的高优先级映射。

不得通过删除生效比价项、否决关系、例外或 JSON 字段来缩短正文，也不得用“其余组”“运行时
查询”等占位说明代替实际保留的映射。

## 固定章节与内容要求

### `# 角色`

明确模型身份、任务目标、左右商品关系、必须逐项执行以及只输出结构化 JSON。保持简洁，不在角色
章节提前展开业务规则。

### `# 输入说明`

必须说明：

- 输入含 `category`，是用 `>` 分隔的多级类目路径；
- `category` 决定哪些比价项生效；
- 使用表格逐行列出比价项及本轮真实 `cateNameTree`；
- 不适用项不抽取、不匹配、不输出、不参与综合判定。

适用范围必须使用本轮规则返回的真实 `cateNameTree`，将 `/` 转成 `>`；不得写“全类目”、简称或
从完整示例复制类目。每个真实比价项必须独占一行并保持 `ruleTableInfo[]` 顺序；适用范围相同也
不得把多个比价项合并到同一单元格。这些是生成和校验约束，不是提示词正文：候选提示词只输出
`category` 说明、真实表格和“不适用的比价项不参与判定”，禁止把本段元规则复制到表格下方。

### `# 执行步骤`

必须完整包含 `Step0` 至 `Step3`，且顺序固定：

- `Step0`：读取 `category`，确定本次生效比价项子集；
- `Step1`：从允许来源逐项抽取左右 `value` 和 `source`；
- `Step2`：按各比价项规则生成 `match` 和 `reason`；
- `Step3`：只能基于生效比价项的 `match` 得出 `result`。

`Step3` 必须写入本轮规则中的综合判定条目及例外，并明确禁止跳过 Step1、Step2 直接给结果。

### `# 判定规则`

#### `## 总原则`

至少覆盖：

- 生效比价项之间的综合关系；
- 高优先级信息不被低优先级覆盖；
- 多来源相同时 `source` 使用 `+` 拼接；
- `result` 和总 `reason` 只能基于生效比价项；
- 禁止引入比价项以外的信息；
- 禁止从编码或模糊线索推断不存在的属性；
- 本轮规则返回的特殊例外。

#### `## 比价项`

每个生效比价项独立使用三级标题并连续编号，只写可信输入实际提供的字段：`infoSource` 写为
“优先级”。“匹配”先使用可归属该项的 `specialRuleContent.ruleContent`，再追加该项真实
`compareLogic` 的兜底语义；已有专属规则也不得省略兜底。专属规则自身引用四种逻辑名称时，按
[rule-transformation-guide.md](rule-transformation-guide.md) 在原位置展开固定语义并保留后续细则。
匹配内容用短句或子项目符号保留必要条件、阈值、默认值和边界。“品牌”和“材质”除外：必须完整
展开可信输入
提供的具名规则、抽取格式、互认/交集规则、无效值、阈值、特殊边界和消歧示例。
无法归属已有比价项的特殊规则写入总原则，禁止据此新增比价项或 `extracted` 字段。部分类目生效时
才补充“适用类目”。不得为了字段齐全补写规则。

删除任一比价项后，后续所有 `### N. 比价项名称` 必须按当前顺序连续前移。例如删除
`### 6. 型号` 后，原 `### 7. 货号` 必须改为 `### 6. 货号`，依次类推。

### `## 映射表`

有效 `compareItem` 包含“品牌”时，必须查询并过滤品牌关系；非空时按固定格式输出，为空时说明查询后无符合当前类目的关系。
有效 `compareItem` 包含“材质”时按材质门禁输出。不得编造条目或从完整示例复制。

### `# 图片说明`

必须说明第 1 张和第 2 张图片分别对应哪一侧商品，并列出本轮允许通过主图判断的生效比价项。
图片只是允许来源之一；高优先级属性或标题已有明确信息时，不得被低优先级图片判断覆盖。

### `# 输出格式`

必须要求只输出合法 JSON，不输出解释、注释或 Markdown 围栏；提供完整、可解析的 JSON 示例，并
逐条写明字段规则。

## 完整格式模板

以下模板是生成候选提示词的直接依据。所有尖括号内容必须替换为本轮真实数据；条件不适用时删除
对应行，不得留下占位符、`TODO` 或空标题。

````markdown
# 角色

你是一个电商同款商品判定专家，负责判断左侧商品与右侧竞品是否为「同款」。严格按照下方【执行步骤】和【判定规则】逐项核查，输出结构化JSON。

---

# 输入说明

左侧商品信息中包含`category`字段，格式为多级类目路径（用`>`分隔），例如
`"<本轮 cateNameTree 将 / 转成 > 后的真实路径>"`。**比价项的适用范围由`category`决定**：

|比价项|适用类目（`category`命中任一前缀即生效）|
|--------|-----------------------------------------|
|<真实比价项1>|<本轮 cateNameTree 将 / 转成 > 后的真实路径>|
|<真实比价项2>|<本轮 cateNameTree 将 / 转成 > 后的真实路径>|

**不适用的比价项不参与判定**：不抽取、不输出到`extracted`、不计入`match`综合判定。

---

# 执行步骤

**Step0：读取category**
—根据上表确定本次需要判定的比价项子集（默认全部<本轮生效比价项数量>项，剔除不适用项）

**Step1：逐项抽取**
—仅对生效比价项，从左右商品的商品属性/商品标题/主图中，按各比价项的优先级提取`value`和`source`，填入`extracted`的`left`/`right`字段

**Step2：逐项匹配**
—对每个生效比价项，按下方匹配规则判定`match`（`true`/`false`）和`reason`

**Step3：综合判定**
—基于所有生效比价项的`match`字段得出`result`：
- <来自当前规则的综合判定条目1>
- <来自当前规则的综合判定条目2>
- <来自当前规则的例外条目，如某项不构成单独否决；没有则删除>
- 严禁跳过Step1、Step2直接给出`result`，必须先完成抽取和逐项匹配。

<仅当真实比价项精确存在“货号”时，以上 Step3 必须替换为：
—基于所有生效比价项的match字段得出result：
- 除货号外所有生效项match=true→**必须判same**（无论货号是否一致）
- 所有生效项match=true→same
- 任一生效项（货号除外）match=false→different
- ⚠️**货号不一致绝对不构成否决**：即使货号match=false，只要其余生效项全部match=true，result必须为same。严禁将货号差异作为判different的理由。
- 严禁跳过Step1/2直接给result。必须先完成抽取和逐项匹配，再得出最终结论。>

---

# 判定规则

## 总原则

1. <来自当前规则的生效比价项综合关系>
2. 高优先级来源有明确信息时，不得用低优先级来源覆盖；多来源信息相同时`source`用`+`拼接。
3. `result`和总`reason`只能基于生效比价项的`match`字段，不得引入比价项以外的信息。
4. `value`必须来自商品属性、商品标题或主图中的明确信息，不得从编码、数字前缀或模糊线索推断。
5. <来自当前规则的特殊例外；没有则删除本行>

## 比价项（最多<本轮生效比价项数量>项，按`category`裁剪）

### 1. <比价项名称>

- **适用类目**：<本轮 cateNameTree 将 / 转成 > 后的真实路径>
- **优先级**：<来自本轮规则的来源顺序>
- **匹配**：<先写可归属本项的完整特殊规则；再写品牌或货号固定规则（命中时）；最后写真实 compareLogic 的兜底语义>

普通比价项逐字段生成，空字段直接删除。先逐条消费 `specialRuleContent.ruleContent`：点名或语义唯一归属已有
比价项时，完整整理为不改变含义的必要短句并写入“匹配”，同一条下的子项、阈值、默认值和特殊
场景不得丢失；无论该项是否存在专属规则，最后都必须按
[rule-transformation-guide.md](rule-transformation-guide.md) 展开真实 `compareLogic` 兜底；其他逻辑
名称保留真实原文，不得自行解释。专属规则自身引用已知逻辑名称时在原位置展开。
无法归属已有比价项时写入总原则，禁止新建比价项。品牌、材质不执行本段压缩：按其可信规则
完整展开多个具名条目。不得用常识补充规则。

### 2. <下一比价项名称>

- **优先级**：<真实来源顺序>
- **匹配**：<真实 compareLogic 和特殊规则的必要短句>
- <按真实规则继续展开必要条目>

按本轮查询结果为每个生效比价项生成一个章节，并保持连续编号。不得输出不存在的比价项，也不得
把所有比价项套成完全相同的空泛四字段。

## 映射表

以下两个子表分别受 `compareItem` 门控，只输出满足条件的子表；两个都不满足时不得编造映射内容。

### 母子品牌映射表（<真实适用类目或业务范围>）

母品牌与子品牌可互相匹配，同一母品牌下的不同子品牌也可匹配：

1. <母品牌>→<子品牌1>|<子品牌2>
2. <母品牌>→<子品牌1>

---

### 材质对照表（同组内可互匹配，跨组不可）

**<材质组名称1>**：<同组材质1>、<同组材质2>、<同组材质3>

**<材质组名称2>**：<同组材质1>、<同组材质2>

---

# 图片说明

第1张=左侧商品主图，第2张=右侧商品主图。图片辅助判断<本轮允许从主图判断的生效比价项>。信息来源仅限三种：商品属性、商品标题、主图。高优先级来源已有明确信息时以其为准。

---

# 输出格式

严格只输出JSON，禁止额外文字、注释或```符号。

**`extracted`中只包含生效的比价项**：不适用的比价项按`category`裁剪后不出现在`extracted`对象中，不抽取、不输出空对象、不参与综合判定。

```json
{
  "result": "same或different",
  "reason": "一句话关键判定依据",
  "confidence": 0.0,
  "extracted": {
    "<生效比价项1>": {
      "left": {
        "value": "",
        "source": ""
      },
      "right": {
        "value": "",
        "source": ""
      },
      "match": true,
      "reason": ""
    },
    "<生效比价项2>": {
      "left": {
        "value": "",
        "source": ""
      },
      "right": {
        "value": "",
        "source": ""
      },
      "match": true,
      "reason": ""
    },
    "key_diff_point": ""
  }
}
```

字段规则：

- `extracted`仅包含按`category`生效的比价项；不适用项整体省略，不输出空对象。
- `confidence`取值范围为`0.0`～`1.0`（含边界）的浮点数，越大表示判定把握越高；示例中的`0.0`仅为占位值。必须输出具体数字字面量（如`0.85`），严禁输出`0.0~1.0`、`0.0-1.0`等区间记法。
- `value`抽取不到时填`"缺失"`，`source`填`""`。
- `source`仅限三种：`商品属性`（商品属性栏）、`商品标题`（商品标题）、`主图`（商品主图）；不属于这三种的来源填`""`。
- `match=true`表示匹配或不影响判断，`match=false`表示不匹配；`match=false`必须能由对应比价项规则解释。
- 多来源信息相同时，`source`使用`+`拼接，如`"商品属性+商品标题"`。
- 材质`value`如果分多部位，必须按部位输出，格式为`帮面:XX;鞋底:YY;鞋垫:ZZ`，缺失的部位省略；服装等其他类目使用本轮规则规定的真实部位名。
- `key_diff_point`始终保留；`result="same"`时必须为空字符串，`result="different"`时填写导致否决的关键差异点。
````

上面的`字段规则：`标题及其下方8条规则是候选提示词的固定必选正文，不是格式说明或可选注释。
生成时必须逐条保留，不得概括、合并、改写或省略；JSON代码围栏结束不代表`# 输出格式`章节结束，
只有8条字段规则全部输出后该章节才完整。具体类目不涉及材质时，也必须保留多部位材质的字段格式规则，
因为它属于输出协议约束。

生成候选正文时，先把`# 输出格式`至第8条`key_diff_point`规则作为一个不可拆分的固定块完整复制，
再生成和填充前面的动态业务章节。不得先生成JSON后再“视剩余长度”决定是否追加字段规则；任何长度
控制都只能压缩动态业务内容，不能裁剪这个固定块。

## 比价项写法细则

### 来源优先级

- 使用 `>` 表示来源优先顺序，使用 `/` 表示同等参考；必须与本轮规则返回含义一致。
- 高优先级来源有明确值时使用该值；只有高优先级缺失或不明确时才看低优先级。
- 多个允许来源给出相同信息时保留合并来源；信息冲突时按优先级选择，不得自行投票。

### 抽取规则

- `value` 必须忠实保留影响判定的名称、数值、单位、含量和部位信息。
- 单一属性可直接输出值；多值或多部位信息按当前比价项规则规定的稳定格式输出。
- 材质存在多成分时完整保留成分与含量；存在多部位时按 `部位:值;部位:值` 输出。
- 图片只用于规则允许从主图判断的比价项，不得从模糊图片推断编码、精确含量或不可见属性。

### 匹配规则

- 先完成左右抽取，再进行归一化和匹配，不得从最终标签倒推抽取值。
- `match` 必须对应当前比价项的规则；`reason` 说明左右值如何命中该规则。
- 默认、缺失、不影响判断、单独不构成否决等语义必须以本轮规则为准，不得从完整示例继承。
- 涉及阈值、集合交集、映射同组、多部位逐项比较或特殊例外时，完整写出条件和边界。

### 规则展开粒度

品牌、材质以外的比价项默认只写`优先级`和`匹配`，缺哪个删哪个；仅部分类目适用时再写`适用类目`。匹配可用
多个短项目符号表达必要条件，但不得展开成长篇说明。抽取、归一化、映射、阈值、多部位、缺失值、
否决和例外等条件也统一放入`匹配`，不得另设栏目；必要时在子项目开头保留简短规则名。

“品牌”和“材质”必须完整展开，允许并应按可信输入使用“基本一致性”“中英文互认”“母子品牌
互认”“同组即匹配”“抽取规则”“交集规则”“未标注含量时”“双方均标注含量时”“多部位材质
匹配”“无效材质”“特殊规则”“映射表参考”等具名条目及必要示例。不得将这些内容压成一行：

- 抽取来源和优先级；
- 归一化、符号处理或同义词；
- 映射互认或同组匹配；
- 数值或含量阈值；
- 多部位、多成分或多品牌处理；
- 无效值和缺失值处理；
- 单独否决、不得单独否决及综合判定例外。

以下情况仅针对品牌、材质以外的普通比价项，视为过度展开并删除；品牌和材质按上方展开例外保留：

- 每个比价项都具有完全相同的一组`抽取规则`、`归一化规则`、`例外与边界`和`具体规则名`；
- 条目内容只是把`优先级`或`匹配`换一种说法重复一遍；
- 条目包含本轮规则未返回的行业常识、示例品牌、推测性归一化方式或宽泛边界；
- 条目、优先级、特殊规则、示例或说明中出现任何大小写形式的 `sku`；
- 将四种已知 `compareLogic` 扩写出固定语义以外的阈值、缺失值处理或行业判断，或擅自解释其他
  未定义逻辑名称；
- 保留不影响执行的背景说明、风险分析、同义重复或可删除而不损失规则含义的示例。

## 映射表写法细则

### 母子品牌

- 有效 `compareItem` 精确包含“品牌”时必须查询并过滤；有有效组时输出非空母子品牌映射表，为 0 组时省略小节并输出
  “本轮品牌关系查询后无符合当前类目的母子品牌映射”。无“品牌”时整个小节必须省略。
- 标题格式：`### 母子品牌映射表（<真实适用类目或业务范围>）`，禁止写动态组数。
- 每个主品牌组独占一行，使用从 1 开始连续递增的阿拉伯数字序号。
- 禁止输出只有序号而没有关系内容的空条目；过滤或删除后先移除空行，再整体重新编号。
- 母品牌与子品牌之间只使用 `→`，多个子品牌之间只使用半角 `|`。
- 禁止使用无序列表、Markdown 加粗、中文冒号、顿号或逗号分隔子品牌。
- 相同主品牌不得拆成多组；同一子品牌关系冲突时停止，不得自行选择。
- 模型自行生成时最多 100 个主品牌组；用户明确新增的关系不受此限制。
- 删除任一母子品牌组后，后续条目必须连续前移并重新编号；例如删除第 6 组后，原第 7 组改为
  第 6 组，直至末项。执行方式是丢弃全部旧序号，按剩余行顺序整体重写为 `1..N`；不得只删除
  目标行而保留后续旧编号。相邻编号不是 `+1`、首项不是 1 或存在重复时均视为未完成，禁止 S3。
  标题不随条目数量变化。
- 仅当材质对照表也需输出时，最后一条母子品牌之后空一行并单独输出 `---`，再空一行输出材质
  对照表标题；禁止加粗分隔线或把 `---` 与任一标题写在同一行。

### 材质分组

- 有效 `compareItem` 精确包含“材质”时必须输出非空材质对照表；否则整个小节必须省略。
- 标题固定为：`### 材质对照表（同组内可互匹配，跨组不可）`。
- 每组使用 `**组名**：成员1、成员2`。分组键、组名和成员严格按
  [rule-loading-policy.md](rule-loading-policy.md) 的 `(parentId,isLeaf)` 算法生成，不得由模型自行聚类。
- 不得把工艺名、营销词或跨行业材料自行归入某组；无法确认时不输出。
- “同组可匹配”是否存在阈值、部位或含量例外，必须以本轮规则为准并写入对应比价项章节。
- 最多 50 个材质组；实际不足时不补齐。

## JSON 输出字段规则

### 顶层字段

- `result`：仅允许当前协议规定的 `same` 或 `different`。
- `reason`：一句话说明综合判定的关键依据，只引用生效比价项。
- `confidence`：`0.0`～`1.0` 的具体数字字面量，不加引号，不写范围。
- `extracted`：对象，只包含生效比价项和始终存在的 `key_diff_point`。

### 每个生效比价项

- `left.value`、`right.value`：左右商品抽取值；抽取不到填 `"缺失"`。
- `left.source`、`right.source`：只允许空字符串或由 `商品属性`、`商品标题`、`主图` 使用 `+` 连接。
- `match`：布尔值，不得写成字符串。
- `reason`：说明该项匹配或不匹配所依据的真实值和规则。

### `key_diff_point`

- 字段始终位于 `extracted` 内并始终保留。
- `same` 时为 `""`。
- `different` 时只写导致最终否决的生效比价项差异，不引入未参与判定的信息。

## 完成检查

生成候选全文后逐项检查，任一项不满足都必须先修复再调用校验工具：

### 结构

- 是否包含全部固定章节并保持正确顺序？
- 一级章节之间是否使用 `---`？
- 是否完整包含 `Step0` 至 `Step3`，且没有跳过抽取或匹配直接给结果？
- 是否删除全部尖括号占位符、`TODO`、空标题和无内容条目？

### 业务规则

- 是否只使用本轮 MCP 返回的比价项、类目、优先级、阈值、例外和映射？
- 是否保留全部生效比价项、否决关系和综合判定例外？
- 是否按 `category` 裁剪，并从规则章节和 JSON 中整体删除不适用项？
- 是否没有从编码、常识、完整示例或相近名称发明业务事实？
- 是否已删除 `radar_query_price_rule` 投影内容中的全部 SKU 相关来源、规则和措辞，且未替换成其他来源？
- 是否先抽取、再匹配、最后综合判断？
- 每个比价项是否先写专属规则、再写对应兜底逻辑，且两者均未遗漏？
- 存在品牌时是否完整写入固定品牌规则；存在货号时是否完整写入货号匹配规则及货号专用 Step3？

### 映射与长度

- 模型自行生成的母子品牌是否不超过 100 组、材质是否不超过 50 组，并已删除低相关、不常见、跨行业与不确定项？用户明确新增的母子品牌是否已直接加入且未受数量限制？
- 母子品牌标题是否为三级标题、只写真实范围且不写组数？是否已丢弃旧序号并整体重写为连续
  `1..N`，格式是否统一为 `母品牌→子品牌1|子品牌2`？
- 材质组名和成员是否来自可信映射，且没有自行归类？
- 全文是否大体在 1 万字以内，且没有为压缩长度删除规则？

### JSON

- JSON 示例是否可解析，且包含 `result`、`reason`、`confidence`、`extracted`？
- `extracted` 是否只包含生效比价项，没有不适用项或空对象？
- 每个比价项是否包含左右 `value/source`、布尔 `match` 和 `reason`？
- `confidence` 是否为范围内的具体数字字面量，而不是字符串或区间？
- `source` 是否只使用三种允许值并按 `+` 拼接？
- 多部位材质是否按 `部位:值;部位:值` 输出？
- `key_diff_point` 是否始终存在，并与 `result` 一致？
- JSON 围栏后是否仍完整包含`字段规则：`及其下方8条固定规则？缺少任一条都不得调用校验工具。
- 每个比价项是否默认只保留必要字段，所有额外条目都有本轮可信规则的直接依据且不与其他条目重复？

字段规则的8个必含语义逐项检查，不能只检查标题：

1. `extracted`仅包含按`category`生效的比价项；
2. `confidence`必须为`0.0`～`1.0`范围内的具体数字字面量；
3. 抽取不到时`value="缺失"`且`source=""`；
4. `source`只允许`商品属性`、`商品标题`、`主图`及其`+`组合；
5. `match`必须为布尔值并说明`true`、`false`含义；
6. 相同信息来自多个来源时使用`+`拼接；
7. 多部位材质使用`部位:值;部位:值`；
8. `key_diff_point`始终保留，并与`result`一致。
